#ifndef HELPERS
#define HELPERS

#include <string>
#include <vector>
#include <array>
#include <cstdint>

void ConsoleLog(const std::string& text);
uint16_t getWordIndex(std::vector<std::string> wordList, std::string targetWord);

#endif
